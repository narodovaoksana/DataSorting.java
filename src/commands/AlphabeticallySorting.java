package commands;
import java.util.ArrayList;
import java.util.Collections;
import java.util.List;
import lb5.Students;
public class AlphabeticallySorting {
        public void (List<Students> students){
            public void  sortAlphabetically(List<Students> students); // сортування за алфавітом
            System.out.println("Відсортовані за алфавітом: " + Arrays.toString(data));
            List<Students> sort = new ArrayList<>();
            for (int i = 0; i < students.size(); i++) {
                sort.add(students.get(i));
            }
            Collections.sort(sort);
            for (int i = 0; i < sort.size(); i++) {
                System.out.println(sort.get(i));
            }
        }
    }
    sortAlphabetically(data); // сортування за алфавітом
        System.out.println("Відсортовані за алфавітом: " + Arrays.toString(data));
